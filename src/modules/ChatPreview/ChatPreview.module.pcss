.root {
  display: grid;
  grid-gap: 16px;
  grid-auto-flow: column;
  grid-auto-columns: 48px 1fr;
}

.avatar {
  width: 48px;
  height: 48px;
  object-fit: cover;
  object-position: top center;
}

.message {
  font-size: 12px;
  color: var(--secondary-text);
}

